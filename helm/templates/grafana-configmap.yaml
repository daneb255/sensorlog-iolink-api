apiVersion: v1
data:
  GF_SERVER_SERVE_FROM_SUB_PATH: "true"
  GF_SERVER_ROOT_URL: {{.Values.grafana.scheme}}://{{.Values.grafana.host}}/grafana
  GF_SERVER_DOMAIN: {{.Values.grafana.host}}
kind: ConfigMap
metadata:
  labels:
    run: env-grafana
  name: env-grafana
